# 稀疏检索 — BM25 关键词检索
# 知识点：BM25 是经典的概率检索模型
#   - 核心思想：一个词在文档中出现越多、在整个语料库越稀有，越重要
#   - 公式：Score(D,Q) = Σ IDF(qi) · (f(qi,D)·(k1+1)) / (f(qi,D)+k1·(1-b+b·|D|/avgdl))
#   - 与 Dense 检索互补：BM25 擅长精确匹配，Dense 擅长语义匹配

这是一份用于测试的样例文档。

# 测试文档标题

这是第一段内容，用于验证 Ingestion Pipeline 的文档加载、切分和向量化功能。

## 第一节

RAG（Retrieval-Augmented Generation）是一种结合检索和生成的技术方案。
核心思想是：先从知识库中检索相关文档片段，再将检索结果作为上下文传递给 LLM 生成答案。

## 第二节

混合检索策略结合了 BM25 稀疏检索和 Dense Embedding 稠密检索，
通过 RRF（Reciprocal Rank Fusion）算法融合两路结果，
在查全率和查准率之间取得平衡。
